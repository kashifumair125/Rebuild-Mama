import 'package:flutter/material.dart';

// TODO: Implement SOS routine screen
// Displays specific relief routine exercises

class SosRoutineScreen extends StatelessWidget {
  const SosRoutineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SOS Routine Screen - TODO: Implement'),
      ),
    );
  }
}
