import 'package:flutter/material.dart';

// TODO: Implement weeks postpartum screen
// Allows user to enter delivery date and calculates weeks postpartum

class WeeksPostpartumScreen extends StatelessWidget {
  const WeeksPostpartumScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Weeks Postpartum Screen - TODO: Implement'),
      ),
    );
  }
}
