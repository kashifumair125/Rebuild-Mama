import 'package:flutter/material.dart';

// TODO: Implement photo progress screen
// Allows users to save and compare progress photos locally

class PhotoProgressScreen extends StatelessWidget {
  const PhotoProgressScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Photo Progress Screen - TODO: Implement'),
      ),
    );
  }
}
